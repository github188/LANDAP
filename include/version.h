/*
 * version.h
 */

/* version of DDP engine in the format (major.minor.build)
 *
 * build number may be extracted from source control program in case.
 * build number is assigned at compilation time by the command (svnversion .)
 */

#define DDP_ENGINE_MAJOR_VERSION  0
#define DDP_ENGINE_MINOR_VERSION  01
#define DDP_ENGINE_BUILD_NUMBER   84
